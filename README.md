Place the wedding photo image here as `wedding-photo.jpg`.

When running the app, the image will be served from `/wedding-photo.jpg`.

If you want to use a different filename, update `heroImagePath` in `src/App.jsx` accordingly.
